/**
 * useTheme Hook
 *
 * Re-export of theme hooks for convenient imports
 */

export { useTheme, useThemeColors } from '../contexts/ThemeContext';
export type { Theme, ThemeColors, ThemeMode } from '../contexts/ThemeContext';
